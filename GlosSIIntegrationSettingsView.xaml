<UserControl x:Class="GlosSIIntegration.GlosSIIntegrationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="Enable GlosSI Integration:"/>
        <CheckBox IsChecked="{Binding Settings.IntegrationEnabled}"/>
        <TextBlock Text="Also close the game when the overlay is closed?"/>
        <CheckBox IsChecked="{Binding Settings.CloseGameWhenOverlayIsClosed}"/>
        <TextBlock Text="Path to GlosSIConfig.exe:"/>
        <TextBox Text="{Binding Settings.GlosSIConfigPath}"/>
        <Button Click="Button_Click">Edit Default GlosSI Target Configuration</Button>
    </StackPanel>
</UserControl>